<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Solitaire Chat" %></title>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <%= favicon_link_tag %>
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="brand" href="/">SolitaireChat</a>
        </div>
      </div>
    </div>

    <div class="container">
      <div class="content">
        <div class="row">
		  <div class="span12">
			<div class="well sidebar-nav">
			    <fieldset>
				  <div class="btn-group">
					<button class="btn" data-event-click="controllers.messages.setNewDeck"><i class="icon-file"></i> New</button>
					<button href="#importModal" role="button" class="btn" data-toggle="modal"><i class="icon-folder-open"></i> Import</button>
					<button href="#exportModal" role="button" class="btn" data-toggle="modal"><i class="icon-hdd"></i> Export</button>
					<button class="btn" data-event-click="controllers.messages.shuffle"><i class="icon-refresh"></i> Shuffle</button>
			  	  </div>
				  <ul class="unstyled inline deck" id="deck"></ul>
				  <div class="clearfix"></div>
				</fieldset>
			</div>
		  </div>
          <div class="span4">
            <div class="well sidebar-nav">
              <ul class="nav nav-list">
                <li class="nav-header">CHATTERS</li>
                <li data-foreach-user="User.all.sortedBy.name" data-bind="user.name | humanize"></li>
              </ul>
            </div>
          </div>
          <div class="span8">
            <form id="message" data-formfor-message="controllers.messages.emptyMessage"
                  data-event-submit="controllers.messages.create">
              <input data-view="MessageContentView"
                     id="message_content"
                     type="text"
                     class="span8"
                     data-bind="message.content"></input>
            </form>
            <div id="messages" class="span8" data-yield="main">
            </div>
          </div>
          <%= yield %>
        </div><!--/row-->
      </div><!--/content-->
    </div> <!-- /container -->
    
    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
